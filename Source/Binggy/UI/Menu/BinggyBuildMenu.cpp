// Fill out your copyright notice in the Description page of Project Settings.


#include "UI/Menu/BinggyBuildMenu.h"

#include "Components/GridPanel.h"

void UBinggyBuildMenu::NativePreConstruct()
{
	Super::NativePreConstruct();
	/*Container->ClearChildren();
	RowIndex = 0;
	ColumnIndex = 0;

	TArray<FName> RowNames = Buildables->GetRowNames();
	for (FName Name : RowNames)
	{
		FBuildable* Buildable = Buildables->FindRow<FBuildable>(Name, "Buildable");
		if (Buildable)
		{
			
		}
	}*/
	
}
